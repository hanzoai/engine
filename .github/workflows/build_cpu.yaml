name: build_cpu

# Neutralized — the native Hanzo pipeline now owns the canonical image build+deploy.
# Build + push of ghcr.io/hanzoai/engine (root Dockerfile) and the App CR rollout
# run in-cluster: Hanzo Git push -> act_runner -> BuildKit -> ghcr.io/hanzoai/engine:<sha>
# -> kubectl patch app engine -> operator reconcile -> hanzocd.
# Canonical pipeline: .hanzo/workflows/deploy.yml. GitHub is a mirror.
on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: hanzo-build-linux-amd64
    steps:
      - run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
